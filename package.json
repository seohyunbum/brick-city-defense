{
  "name": "brick-city-defense",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "verify:static": "node scripts/quality-gate.mjs",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run verify:static && npm run smoke && npm run smoke:story && npm run smoke:pilot",
    "smoke:story": "node scripts/smoke-story.mjs",
    "smoke:pilot": "node scripts/smoke-pilot.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
